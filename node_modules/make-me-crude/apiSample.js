/*
    Create
*/

myCrud
    .$create({
        userID: 'user1',
        name: 'Daniel Bastos'
    })
    .then(function( doc ){
        console.log( doc );
    })
    .fail(function( err ){
        console.log( doc );
    });

/*
    Read
*/

myCrud
    .$where({
        userID: 'user1'
    })
    .$read(['list', 'of', 'fields'])
    .then(function( doc ){
        console.log( doc );
    })
    .fail(function( err ){
        console.log( doc );
    });

/*
    Update
*/
myCrud
    .$where({
        userID: 'user1'
    })
    .$update({
        name: 'Daniel Bastos Albano Nascimento'
    })
    .then(function( doc ){
        console.log( doc );
    })
    .fail(function( err ){
        console.log( doc );
    });


/*
    Delete
*/

myCrud
    .$where({
        userID: 'user1'
    })
    .$delete()
    .then(function( doc ){
        console.log( doc );
    })
    .fail(function( err ){
        console.log( doc );
    });

/*
    Query
*/

myCrud
    .$where({
        userID: 'someCondition'
    })
    .$query(['list', 'of', 'fields'])
    .then(function( doc ){
        console.log( doc );
    })
    .fail(function( err ){
        console.log( doc );
    });

/*
    SubCRUD - Create
*/

myCrud
    .$where({
        userID: 'user1'
    })
    .posts
    .$create({
        title: 'How to master everything',
        body: 'Never gonna give you up!'
    })
    .then(function( doc ){
        console.log( doc );
    })
    .fail(function( err ){
        console.log( doc );
    });

/*
    SubCRUD - Update
*/

var subCrud = myCrud.$where( {userID: myID} ).post;

myCrud
    .$where({
        userID: 'user1'
    })
    .posts
    .$where({
        title: 'How to master everything'
    })
    .$update({
        body: 'Just decided to give you up'
    })
    .then(function( doc ){

    })
    .fail(function( err ));
